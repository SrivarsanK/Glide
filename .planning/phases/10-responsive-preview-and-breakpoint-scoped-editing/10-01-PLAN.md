---
phase: 10-responsive-preview-and-breakpoint-scoped-editing
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/writer.ts
  - src/writer.test.ts
autonomous: true
requirements:
  - RESP-02
user_setup: []

must_haves:
  truths:
    - "AST class rewriter scopes updates using Tailwind breakpoint prefixes"
    - "TypeScript compilation passes for writer module modifications"
  artifacts:
    - path: src/writer.ts
      provides: "updateClassString supporting active breakpoint prefixes"
      contains: "breakpoint"
    - path: src/writer.test.ts
      provides: "Unit tests verifying responsive class name updates"
      contains: "breakpoint"
---

<objective>
Enhance the Tailwind class rewriter (`updateClassString` and `updateClassName`) in `src/writer.ts` to accept an optional active breakpoint parameter, targeting only the scoped class names (e.g. `md:ml-4`) while leaving other breakpoints untouched.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-responsive-preview-and-breakpoint-scoped-editing/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Tailwind class rewriter with breakpoint support</name>
  <files>src/writer.ts</files>
  <action>Modify updateClassString and updateClassName in src/writer.ts to accept an optional breakpoint string parameter. Filter and insert class names prefixed with breakpoint: when active.</action>
  <verify>Check writer.ts compiles</verify>
  <acceptance_criteria>
    - src/writer.ts compiles successfully
    - updateClassString supports active breakpoints (sm, md, lg, etc.)
  </acceptance_criteria>
  <done>Tailwind class rewriter updated with breakpoint support</done>
</task>

<task type="auto">
  <name>Task 2: Write tests for responsive class name updates</name>
  <files>src/writer.test.ts</files>
  <action>Add unit tests in src/writer.test.ts asserting that editing padding or margin at specific breakpoints adds/replaces prefixed classes and leaves default/other breakpoints intact.</action>
  <verify>Run tests using npm run test to verify success</verify>
  <acceptance_criteria>
    - src/writer.test.ts contains responsive tests
    - all tests pass
  </acceptance_criteria>
  <done>Responsive class updates tests implemented and passing</done>
</task>

</tasks>

<verification>
- [ ] TypeScript type checks succeed
- [ ] Test command `npm run test` executes successfully and reports all tests passing
</verification>

<success_criteria>
- Edits are scoped correctly to active Tailwind breakpoints
- Tests confirm RESP-02 is satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/10-responsive-preview-and-breakpoint-scoped-editing/10-01-SUMMARY.md`
</output>
