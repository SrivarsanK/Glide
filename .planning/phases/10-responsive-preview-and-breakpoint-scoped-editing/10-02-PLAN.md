---
phase: 10-responsive-preview-and-breakpoint-scoped-editing
plan: "02"
type: execute
wave: 2
depends_on:
  - plan: "01"
files_modified:
  - src/viewport.ts
  - src/viewport.test.ts
autonomous: true
requirements:
  - RESP-01
user_setup: []

must_haves:
  truths:
    - "Viewport utility maps widths to correct active Tailwind breakpoint names"
    - "TypeScript compilation passes for viewport module"
  artifacts:
    - path: src/viewport.ts
      provides: "Viewport resolution utilities"
      contains: "resolveActiveBreakpoint"
    - path: src/viewport.test.ts
      provides: "Unit tests verifying device width matching"
      contains: "describe"
---

<objective>
Implement the responsive device resolution utility (`resolveActiveBreakpoint`) in `src/viewport.ts` to map browser viewport widths to responsive breakpoints (Mobile, Tablet, Desktop) and return active Tailwind prefixes.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-responsive-preview-and-breakpoint-scoped-editing/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement viewport device resolver</name>
  <files>src/viewport.ts</files>
  <action>Create src/viewport.ts. Define viewport presets and resolveActiveBreakpoint function that maps width (e.g., 800) to its active breakpoint ('md').</action>
  <verify>Check viewport.ts compiles</verify>
  <acceptance_criteria>
    - src/viewport.ts exists and compiles successfully
    - contains resolveActiveBreakpoint function
  </acceptance_criteria>
  <done>Viewport device resolver implemented</done>
</task>

<task type="auto">
  <name>Task 2: Write tests for viewport resolver</name>
  <files>src/viewport.test.ts</files>
  <action>Create src/viewport.test.ts. Test matching various widths against standard Tailwind breakpoints (sm: 640px, md: 768px, lg: 1024px, xl: 1280px).</action>
  <verify>Run tests using npm run test to verify success</verify>
  <acceptance_criteria>
    - src/viewport.test.ts exists
    - tests verify active breakpoint resolution
    - all tests pass
  </acceptance_criteria>
  <done>Viewport resolver tests implemented and passing</done>
</task>

</tasks>

<verification>
- [ ] TypeScript type checks succeed
- [ ] Test command `npm run test` executes successfully and reports all tests passing
</verification>

<success_criteria>
- Viewport width resolution maps correctly to responsive breakpoint settings
- Tests confirm RESP-01 is satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/10-responsive-preview-and-breakpoint-scoped-editing/10-02-SUMMARY.md`
</output>
