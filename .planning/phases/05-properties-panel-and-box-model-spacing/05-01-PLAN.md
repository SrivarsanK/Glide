---
phase: 05-properties-panel-and-box-model-spacing
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/properties.ts
  - src/properties.test.ts
autonomous: true
requirements:
  - PROP-01
user_setup: []

must_haves:
  truths:
    - "Properties module parses Tailwind spacing, sizing, positioning and rotation classes successfully"
    - "TypeScript compilation passes for properties module"
  artifacts:
    - path: src/properties.ts
      provides: "Tailwind properties parser and class generator"
      contains: "parseTailwindClasses"
    - path: src/properties.test.ts
      provides: "Unit tests verifying parsing accuracy and overrides"
      contains: "describe"
---

<objective>
Implement the properties parser engine in `src/properties.ts` to parse styling and layout properties (width, height, position, rotation, offsets) from Tailwind utility class strings, and generate updated class strings.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-properties-panel-and-box-model-spacing/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Tailwind class parser</name>
  <files>src/properties.ts</files>
  <action>Create src/properties.ts and implement parseTailwindClasses. Extract padding, margin, width, height, position, offsets, and rotation values from a space-separated string of classes, supporting overrides (e.g. pt-4 overrides py-2).</action>
  <verify>Check properties.ts type checks</verify>
  <acceptance_criteria>
    - src/properties.ts exists and compiles successfully
    - contains parseTailwindClasses method
    - handles overlapping shorthand and specific classes (like px-2 and pl-4) correctly
  </acceptance_criteria>
  <done>Tailwind class parser implemented</done>
</task>

<task type="auto">
  <name>Task 2: Write tests for property parsing</name>
  <files>src/properties.test.ts</files>
  <action>Create src/properties.test.ts and add test cases validating styling class parsing, resolving overrides, and fallback defaults.</action>
  <verify>Run tests using npm run test to verify success</verify>
  <acceptance_criteria>
    - src/properties.test.ts exists
    - tests verify all spacing/sizing properties are parsed accurately
    - all tests pass
  </acceptance_criteria>
  <done>Property parsing tests implemented and passing</done>
</task>

</tasks>

<verification>
- [ ] TypeScript type checks succeed
- [ ] Test command `npm run test` executes successfully and reports all tests passing
</verification>

<success_criteria>
- Tailwind classes are resolved to layout structures
- Tests confirm PROP-01 is satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-properties-panel-and-box-model-spacing/05-01-SUMMARY.md`
</output>
