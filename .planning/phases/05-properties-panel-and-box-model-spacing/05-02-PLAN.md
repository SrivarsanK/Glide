---
phase: 05-properties-panel-and-box-model-spacing
plan: "02"
type: execute
wave: 2
depends_on:
  - plan: "01"
files_modified:
  - src/properties.ts
  - src/properties.test.ts
autonomous: true
requirements:
  - PROP-03
user_setup: []

must_haves:
  truths:
    - "Updating BoxModelProperties produces corrected, clean Tailwind class lists without leaving dead classes"
    - "TypeScript compilation passes for updated properties module"
  artifacts:
    - path: src/properties.ts
      provides: "updateTailwindClasses utility"
      contains: "updateTailwindClasses"
    - path: src/properties.test.ts
      provides: "Unit tests validating spacing class updating and preservation of unrelated classes"
      contains: "updateTailwindClasses"
---

<objective>
Implement class updater functionality (`updateTailwindClasses`) in `src/properties.ts` to replace padding/margin configurations in Tailwind class lists and verify with extensive tests.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-properties-panel-and-box-model-spacing/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Tailwind class updater</name>
  <files>src/properties.ts</files>
  <action>Add updateTailwindClasses to src/properties.ts. This function takes an original class string and a partial BoxModelProperties object, removes old spacing/sizing/position classes, and appends the new ones while preserving unrelated utility classes.</action>
  <verify>Check properties.ts type checks</verify>
  <acceptance_criteria>
    - src/properties.ts compiles successfully
    - contains updateTailwindClasses method
    - leaves unrelated classes (e.g. bg-red-500, text-center) unmodified
  </acceptance_criteria>
  <done>Tailwind class updater implemented</done>
</task>

<task type="auto">
  <name>Task 2: Write tests for class updates</name>
  <files>src/properties.test.ts</files>
  <action>Add test cases to src/properties.test.ts verifying updateTailwindClasses behaves correctly when adding, replacing, or removing padding and margin options.</action>
  <verify>Run tests using npm run test to verify success</verify>
  <acceptance_criteria>
    - src/properties.test.ts contains update tests
    - tests verify padding and margin updates
    - all tests pass
  </acceptance_criteria>
  <done>Class update tests implemented and passing</done>
</task>

</tasks>

<verification>
- [ ] TypeScript type checks succeed
- [ ] Test command `npm run test` executes successfully and reports all tests passing
</verification>

<success_criteria>
- Spacing classes are updated non-destructively
- Tests confirm PROP-03 is satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-properties-panel-and-box-model-spacing/05-02-SUMMARY.md`
</output>
