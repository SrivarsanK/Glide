---
phase: 02-vite-plugin-and-source-stamping
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/plugin.ts
  - src/plugin.test.ts
autonomous: true
requirements:
  - TRANS-01
  - TRANS-02
user_setup: []

must_haves:
  truths:
    - "Vite plugin adds data-cf-source attributes to JSX/TSX elements in development"
    - "Vite plugin does not add attributes when building for production (command !== 'serve')"
    - "TypeScript compilation compiles src/plugin.ts successfully"
  artifacts:
    - path: src/plugin.ts
      provides: "Vite source position stamping plugin"
      contains: "glideSourceStamping"
    - path: src/plugin.test.ts
      provides: "Unit and integration tests for source position stamping"
      contains: "data-cf-source"
---

<objective>
Implement a Vite plugin that stamps JSX/TSX elements in development mode with a `data-cf-source` attribute containing their original relative path, line, and column coordinates. Ensure that the transformation is skipped in production builds.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-vite-plugin-and-source-stamping/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Babel core dependencies to package.json</name>
  <files>package.json</files>
  <action>Add `@babel/core` and `@types/babel__core` to devDependencies in package.json to support typescript Babel compilation. Run npm install to fetch new dependencies.</action>
  <verify>Check package.json contains @babel/core and types</verify>
  <acceptance_criteria>
    - package.json has devDependencies for "@babel/core" and "@types/babel__core"
  </acceptance_criteria>
  <done>package.json updated and dependencies installed</done>
</task>

<task type="auto">
  <name>Task 2: Implement Vite source stamping plugin</name>
  <files>src/plugin.ts</files>
  <action>Create src/plugin.ts containing the Vite plugin `glideSourceStamping`. The plugin must hook into `transform` and traverse `.jsx`/`.tsx` files using `@babel/core` to append the `data-cf-source="relative/path:line:col"` attribute to all JSX opening elements. Ensure path resolution is relative to the project root and is OS-independent. Verify that it returns null if serving in production (command !== 'serve').</action>
  <verify>Check plugin file compiles and contains plugin definition</verify>
  <acceptance_criteria>
    - src/plugin.ts exists and compiles successfully
    - contains "glideSourceStamping" function returning a Vite plugin
    - retrieves start.line and start.column from JSXOpeningElement AST
    - relative file paths are normalized (using forward slashes)
  </acceptance_criteria>
  <done>Vite source stamping plugin implemented</done>
</task>

<task type="auto">
  <name>Task 3: Add unit tests for the Vite plugin</name>
  <files>src/plugin.test.ts</files>
  <action>Create src/plugin.test.ts containing tests verifying that the plugin transforms a JSX string correctly in development, adds correct data-cf-source coordinates, and skips transformation in production mode.</action>
  <verify>Run tests using npm run test to verify success</verify>
  <acceptance_criteria>
    - src/plugin.test.ts exists
    - tests verify JSX transformation with data-cf-source attribute insertion
    - tests verify production mode (serve/build config resolution) behaves as a no-op
    - all tests pass successfully
  </acceptance_criteria>
  <done>Vite plugin unit tests created and passing</done>
</task>

</tasks>

<verification>
- [ ] TypeScript type checks succeed on the project
- [ ] Test command `npm run test` executes and reports passing tests for the plugin
</verification>

<success_criteria>
- package.json devDependencies are configured
- src/plugin.ts compiles and functions
- src/plugin.test.ts passes all checks verifying TRANS-01 and TRANS-02
</success_criteria>

<output>
After completion, create `.planning/phases/02-vite-plugin-and-source-stamping/02-01-SUMMARY.md`
</output>
