---
phase: 04-basic-overlay-canvas-react-tailwind
plan: "02"
type: execute
wave: 2
depends_on:
  - plan: "01"
files_modified:
  - src/overlay.ts
  - src/overlay.test.ts
autonomous: true
requirements:
  - OVERLAY-02
user_setup: []

must_haves:
  truths:
    - "Overlay layer listens for message events and updates bounding box coordinates"
    - "Dragging handles calculates size deltas successfully"
    - "TypeScript compilation passes for overlay module"
  artifacts:
    - path: src/overlay.ts
      provides: "GlideOverlay canvas layer rendering boundaries and resize handles"
      contains: "GlideOverlay"
    - path: src/overlay.test.ts
      provides: "Unit and event integration tests for GlideOverlay"
      contains: "expect"
---

<objective>
Implement the canvas overlay class (`GlideOverlay`) in `src/overlay.ts` to render visual selection boundaries and 8-directional resize handles over selected elements. Intercept drag events on handles to calculate scale/size modifications.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-basic-overlay-canvas-react-tailwind/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement overlay renderer</name>
  <files>src/overlay.ts</files>
  <action>Create src/overlay.ts implementing GlideOverlay class. The class should create an SVG or HTML overlay container, listen for postMessage events from the app frame, and draw/position a selection boundary box and 8 resize handles representing the element's client dimensions.</action>
  <verify>Check overlay.ts compiles</verify>
  <acceptance_criteria>
    - src/overlay.ts exists and compiles successfully
    - exports GlideOverlay class
    - renders visual boundaries and handles on coordinate updates
  </acceptance_criteria>
  <done>Overlay renderer implemented</done>
</task>

<task type="auto">
  <name>Task 2: Handle pointer drag resize events</name>
  <files>src/overlay.ts</files>
  <action>Implement pointer event listeners on the 8 resize handles in src/overlay.ts. When a handle is dragged, compute pointer position deltas, calculate updated width, height, and offset margins, and invoke a resize callback containing the new dimensions.</action>
  <verify>Check handle drag calculates dimensions</verify>
  <acceptance_criteria>
    - calculates positive and negative resizing deltas correctly across all 8 handles
    - calls callback with adjusted dimensions on drag
  </acceptance_criteria>
  <done>Pointer drag resize logic implemented</done>
</task>

<task type="auto">
  <name>Task 3: Write tests for GlideOverlay</name>
  <files>src/overlay.test.ts</files>
  <action>Create src/overlay.test.ts. Add tests verifying that receiving element-selected messages updates the selection coordinate states, and that simulated pointer movements on handles calculate correct size changes.</action>
  <verify>Run tests using npm run test to verify success</verify>
  <acceptance_criteria>
    - src/overlay.test.ts exists
    - tests verify coordinates update on postMessage
    - tests verify resize delta math on handle dragging
    - all tests pass successfully
  </acceptance_criteria>
  <done>GlideOverlay tests created and passing</done>
</task>

</tasks>

<verification>
- [ ] TypeScript type checks succeed on the project
- [ ] Test command `npm run test` executes and reports passing tests for the overlay
</verification>

<success_criteria>
- Overlay boundary aligns on selection messages
- Dragging handles produces correct dimensions
- Tests confirm OVERLAY-02 is fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/04-basic-overlay-canvas-react-tailwind/04-02-SUMMARY.md`
</output>
