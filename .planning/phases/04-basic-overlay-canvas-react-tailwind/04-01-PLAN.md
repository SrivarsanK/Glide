---
phase: 04-basic-overlay-canvas-react-tailwind
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bridge.ts
  - src/bridge.test.ts
autonomous: true
requirements:
  - OVERLAY-01
user_setup: []

must_haves:
  truths:
    - "Bridge script sets up message and click event handlers on the DOM"
    - "Hovering elements with data-cf-source dispatches postMessage to parent"
    - "TypeScript compilation passes for bridge module"
  artifacts:
    - path: src/bridge.ts
      provides: "Injected overlay-bridge for DOM selection telemetry"
      contains: "GlideBridge"
    - path: src/bridge.test.ts
      provides: "Unit and event integration tests for GlideBridge"
      contains: "postMessage"
---

<objective>
Implement the client-side bridge script (`GlideBridge`) in `src/bridge.ts` to run inside the application frame, listen to pointer/click events, retrieve element coordinates and source tags, and communicate this back to the parent window.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-basic-overlay-canvas-react-tailwind/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement DOM interaction bridge</name>
  <files>src/bridge.ts</files>
  <action>Create src/bridge.ts. Implement GlideBridge class that listens to mousemove (hover) and click events. When an element carrying data-cf-source is interacted with, compute its client bounding rect and dispatch a postMessage payload containing the rect coordinates and the source coordinate string to the parent window.</action>
  <verify>Check bridge.ts compiles</verify>
  <acceptance_criteria>
    - src/bridge.ts exists and compiles successfully
    - contains GlideBridge implementation
    - listens for hover and click events
    - dispatches postMessage payloads
  </acceptance_criteria>
  <done>DOM interaction bridge script created</done>
</task>

<task type="auto">
  <name>Task 2: Write tests for GlideBridge</name>
  <files>src/bridge.test.ts</files>
  <action>Create src/bridge.test.ts. Set up JSDOM or mock window/document environment. Add tests verifying that hover/clicks on dummy HTML elements carrying data-cf-source attributes trigger postMessage dispatches with correct bounds.</action>
  <verify>Run tests using npm run test to verify success</verify>
  <acceptance_criteria>
    - src/bridge.test.ts exists
    - tests verify event capture and message sending
    - all tests pass successfully
  </acceptance_criteria>
  <done>GlideBridge tests created and passing</done>
</task>

</tasks>

<verification>
- [ ] TypeScript type checks succeed on the project
- [ ] Test command `npm run test` executes and reports passing tests for the bridge
</verification>

<success_criteria>
- DOM events are mapped to coordinate payloads
- PostMessage sends accurate bounding rects
- Tests confirm OVERLAY-01 is satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/04-basic-overlay-canvas-react-tailwind/04-01-SUMMARY.md`
</output>
