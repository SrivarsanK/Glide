---
phase: 08-undo-redo-and-pretext-integration
plan: "02"
type: execute
wave: 2
depends_on:
  - plan: "01"
files_modified:
  - src/text.ts
  - src/text.test.ts
autonomous: true
requirements:
  - TEXT-01
  - TEXT-02
user_setup: []

must_haves:
  truths:
    - "Text measurement resolves line count and segments using @chenglou/pretext"
    - "TypeScript compilation passes for text module"
  artifacts:
    - path: src/text.ts
      provides: "Pretext text measurement and wrapping inspector"
      contains: "measureTextLayout"
    - path: src/text.test.ts
      provides: "Unit tests verifying width calculations and segmentation lines"
      contains: "describe"
---

<objective>
Implement text measurement and layout wrapping utility (`measureTextLayout`) in `src/text.ts` using `@chenglou/pretext` to prevent visual text overflow or clipping.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-undo-redo-and-pretext-integration/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement pretext wrapper</name>
  <files>src/text.ts</files>
  <action>Create src/text.ts. Polyfill OffscreenCanvas. Implement measureTextLayout using pretext prepareWithSegments and layoutWithLines, returning lineCount, lines array, and natural width.</action>
  <verify>Check text.ts compiles</verify>
  <acceptance_criteria>
    - src/text.ts exists and compiles successfully
    - contains measureTextLayout function
  </acceptance_criteria>
  <done>Pretext wrapper implemented</done>
</task>

<task type="auto">
  <name>Task 2: Write tests for text measurement</name>
  <files>src/text.test.ts</files>
  <action>Create src/text.test.ts. Test natural width measurements and multi-line wrapping boundaries.</action>
  <verify>Run tests using npm run test to verify success</verify>
  <acceptance_criteria>
    - src/text.test.ts exists
    - tests verify line counts and wrapping outputs
    - all tests pass
  </acceptance_criteria>
  <done>Text measurement tests implemented and passing</done>
</task>

</tasks>

<verification>
- [ ] TypeScript type checks succeed
- [ ] Test command `npm run test` executes successfully and reports all tests passing
</verification>

<success_criteria>
- Text sizing and wrapping boundaries are resolved
- Tests confirm TEXT-01 and TEXT-02 are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/08-undo-redo-and-pretext-integration/08-02-SUMMARY.md`
</output>
