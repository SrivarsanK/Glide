---
phase: 09-css-modules-vue-adapter
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vue.ts
  - src/vue.test.ts
autonomous: true
requirements:
  - ADAP-02
user_setup: []

must_haves:
  truths:
    - "Vue SFC parser correctly isolates and updates element classes in template blocks"
    - "TypeScript compilation passes for vue module"
  artifacts:
    - path: src/vue.ts
      provides: "Vue SFC template transformer"
      contains: "updateVueSFCClass"
    - path: src/vue.test.ts
      provides: "Unit tests verifying Vue SFC element updates"
      contains: "describe"
---

<objective>
Implement Vue Single File Component (SFC) transformation function (`updateVueSFCClass`) in `src/vue.ts` to parse, locate elements by ID in the template, and update their styling class values.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-css-modules-vue-adapter/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Vue SFC template updater</name>
  <files>src/vue.ts</files>
  <action>Create src/vue.ts. Use @vue/compiler-sfc parse method to extract the template location range. Find the tag carrying data-cf-source matching the target ID and replace/update its class attributes, inserting new values back into sfcCode.</action>
  <verify>Check vue.ts compiles</verify>
  <acceptance_criteria>
    - src/vue.ts exists and compiles successfully
    - contains updateVueSFCClass function
    - correctly modifies class names inside the isolated template block
  </acceptance_criteria>
  <done>Vue SFC template updater implemented</done>
</task>

<task type="auto">
  <name>Task 2: Write tests for Vue SFC updater</name>
  <files>src/vue.test.ts</files>
  <action>Create src/vue.test.ts. Test updating elements with and without pre-existing class attributes inside standard template layouts.</action>
  <verify>Run tests using npm run test to verify success</verify>
  <acceptance_criteria>
    - src/vue.test.ts exists
    - tests verify class name modifications inside SFC string templates
    - all tests pass
  </acceptance_criteria>
  <done>Vue SFC updater tests implemented and passing</done>
</task>

</tasks>

<verification>
- [ ] TypeScript type checks succeed
- [ ] Test command `npm run test` executes successfully and reports all tests passing
</verification>

<success_criteria>
- Vue SFC template elements are mutated safely
- Tests confirm ADAP-02 is satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/09-css-modules-vue-adapter/09-01-SUMMARY.md`
</output>
