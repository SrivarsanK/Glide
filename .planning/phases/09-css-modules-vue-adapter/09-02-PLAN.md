---
phase: 09-css-modules-vue-adapter
plan: "02"
type: execute
wave: 2
depends_on:
  - plan: "01"
files_modified:
  - src/css.ts
  - src/css.test.ts
autonomous: true
requirements:
  - PROP-01
  - PROP-03
user_setup: []

must_haves:
  truths:
    - "CSS Modules modifier updates class rules in CSS files non-destructively"
    - "TypeScript compilation passes for css module"
  artifacts:
    - path: src/css.ts
      provides: "CSS Modules stylesheet mutator"
      contains: "updateCSSModuleRule"
    - path: src/css.test.ts
      provides: "Unit tests verifying CSS rules creation and property edits"
      contains: "describe"
---

<objective>
Implement the CSS Modules stylesheet mutator (`updateCSSModuleRule`) in `src/css.ts` to locate styling class blocks and add/replace styling properties (such as padding, margins, sizes, positioning).
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-css-modules-vue-adapter/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement CSS Modules mutator</name>
  <files>src/css.ts</files>
  <action>Create src/css.ts. Implement updateCSSModuleRule which scans for a class name block, adds or replaces CSS properties, and appends the rule at the bottom if not present in stylesheet.</action>
  <verify>Check css.ts compiles</verify>
  <acceptance_criteria>
    - src/css.ts exists and compiles successfully
    - contains updateCSSModuleRule function
  </acceptance_criteria>
  <done>CSS Modules mutator implemented</done>
</task>

<task type="auto">
  <name>Task 2: Write tests for CSS Modules mutator</name>
  <files>src/css.test.ts</files>
  <action>Create src/css.test.ts. Verify modifying existing properties, adding new properties, and generating new CSS rule blocks.</action>
  <verify>Run tests using npm run test to verify success</verify>
  <acceptance_criteria>
    - src/css.test.ts exists
    - tests verify CSS code rule modifications
    - all tests pass
  </acceptance_criteria>
  <done>CSS Modules mutator tests implemented and passing</done>
</task>

</tasks>

<verification>
- [ ] TypeScript type checks succeed
- [ ] Test command `npm run test` executes successfully and reports all tests passing
</verification>

<success_criteria>
- CSS Module stylesheets are modified non-destructively
- Tests confirm PROP-01 and PROP-03 CSS Modules updates are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/09-css-modules-vue-adapter/09-02-SUMMARY.md`
</output>
