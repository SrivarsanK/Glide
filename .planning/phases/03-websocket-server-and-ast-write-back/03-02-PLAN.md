---
phase: 03-websocket-server-and-ast-write-back
plan: "02"
type: execute
wave: 2
depends_on:
  - plan: "01"
files_modified:
  - src/writer.ts
  - src/writer.test.ts
autonomous: true
requirements:
  - SERVER-02
  - ADAP-01
user_setup: []

must_haves:
  truths:
    - "AST parser accurately locates the JSX element matching line and column"
    - "AST writer updates target className attribute while leaving formatting and comments untouched"
    - "TypeScript compilation passes for writer module"
  artifacts:
    - path: src/writer.ts
      provides: "Glide AST writer and React/Tailwind adapter module"
      contains: "updateClassName"
    - path: src/writer.test.ts
      provides: "Unit and integration tests for source write-back"
      contains: "expect"
---

<objective>
Implement the AST parser, traverse helper, and non-destructive writer in `src/writer.ts` to modify JSX `className` attributes on disk at precise source code locations, satisfying React/Tailwind write-back requirements.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-websocket-server-and-ast-write-back/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement coordinates node locator</name>
  <files>src/writer.ts</files>
  <action>Implement AST traversal and parse helper in src/writer.ts. Parse source strings using @babel/parser and use @babel/traverse to locate the JSXOpeningElement matching target 1-indexed line and column coordinates. Return the node path or node range.</action>
  <verify>Check finder helper resolves coordinate nodes</verify>
  <acceptance_criteria>
    - src/writer.ts parses source strings without errors using @babel/parser
    - has a helper function that returns the target JSXOpeningElement AST node at given coordinates
  </acceptance_criteria>
  <done>Coordinates locator helper implemented</done>
</task>

<task type="auto">
  <name>Task 2: Implement non-destructive className writer</name>
  <files>src/writer.ts</files>
  <action>Implement the write-back logic. If className is present, find its attribute range in the original string and replace it. If missing, inject a className attribute directly into the tag string slice. Merge or replace specific CSS classes (e.g. replacing margin classes when a new margin value is passed) without disturbing indentation, comments, or other properties.</action>
  <verify>Check write-back modifies code string non-destructively</verify>
  <acceptance_criteria>
    - modifies only the targeted class attribute slice
    - leaves comments, formatting, and unrelated lines completely untouched
  </acceptance_criteria>
  <done>Non-destructive AST write-back logic implemented</done>
</task>

<task type="auto">
  <name>Task 3: Write tests for AST writer</name>
  <files>src/writer.test.ts</files>
  <action>Create src/writer.test.ts. Add tests verifying AST lookup matching coordinates, successful class insertion/modification, and that comments and formatting are preserved identically.</action>
  <verify>Run tests using npm run test to verify success</verify>
  <acceptance_criteria>
    - src/writer.test.ts exists
    - tests verify coordinate resolution, class modification, and comment preservation
    - all tests pass successfully
  </acceptance_criteria>
  <done>AST writer tests created and passing</done>
</task>

</tasks>

<verification>
- [ ] TypeScript type checks succeed on the project
- [ ] Test command `npm run test` executes and reports passing tests for the writer
</verification>

<success_criteria>
- JSX elements are successfully located by line/col coordinates
- target className updates correctly and safely on disk
- tests confirm SERVER-02 and ADAP-01 are fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/03-websocket-server-and-ast-write-back/03-02-SUMMARY.md`
</output>
