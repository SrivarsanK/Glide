---
phase: 03-websocket-server-and-ast-write-back
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server.ts
  - src/server.test.ts
autonomous: true
requirements:
  - SERVER-01
user_setup: []

must_haves:
  truths:
    - "WebSocket server starts on default port 7777"
    - "Server handles edit message structure successfully"
    - "TypeScript compilation passes for server module"
  artifacts:
    - path: src/server.ts
      provides: "Glide local server managing WebSocket communication"
      contains: "GlideServer"
    - path: src/server.test.ts
      provides: "Unit and connection integration tests for GlideServer"
      contains: "WebSocket"
---

<objective>
Implement the WebSocket server module (`GlideServer`) in development to listen for connection events, receive JSON edit payload messages from the client overlay, and route those payloads correctly.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-websocket-server-and-ast-write-back/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement GlideServer</name>
  <files>src/server.ts</files>
  <action>Create src/server.ts defining the GlideServer class. It should instantiate a WebSocket Server (from 'ws') on port 7777 by default, support starting/stopping, and trigger callbacks or events when connection, disconnection, or messages are received.</action>
  <verify>Check server.ts compiles</verify>
  <acceptance_criteria>
    - src/server.ts exists and compiles
    - exports GlideServer class
    - configures port 7777 as default
  </acceptance_criteria>
  <done>GlideServer class created</done>
</task>

<task type="auto">
  <name>Task 2: Handle incoming edit payloads</name>
  <files>src/server.ts</files>
  <action>Extend GlideServer to handle client messages. Parse JSON messages and validate they match the edit payload format. Trigger a registered edit handler callback (passing file path, line, column, and change data) and reply with a success/error status message.</action>
  <verify>Check message handler parses edit messages</verify>
  <acceptance_criteria>
    - server parses JSON payloads safely
    - calls registered edit handler callback with parsed details
    - replies with success/error status messages
  </acceptance_criteria>
  <done>Message parser and router implemented</done>
</task>

<task type="auto">
  <name>Task 3: Write tests for GlideServer</name>
  <files>src/server.test.ts</files>
  <action>Create src/server.test.ts. Start a local server instance, connect a mock WebSocket client, send an edit payload, and verify the edit handler is triggered and the client receives a success status message.</action>
  <verify>Run tests using npm run test to verify success</verify>
  <acceptance_criteria>
    - src/server.test.ts exists
    - tests verify server starts, accepts connection, receives message, and calls handler
    - all tests pass successfully
  </acceptance_criteria>
  <done>WebSocket server tests created and passing</done>
</task>

</tasks>

<verification>
- [ ] TypeScript type checks succeed on the project
- [ ] Test command `npm run test` executes and reports passing tests for the server
</verification>

<success_criteria>
- GlideServer class is implemented
- Local WebSocket connection accepts messages and responds
- Tests confirm SERVER-01 is fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/03-websocket-server-and-ast-write-back/03-01-SUMMARY.md`
</output>
