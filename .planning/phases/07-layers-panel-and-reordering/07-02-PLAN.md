---
phase: 07-layers-panel-and-reordering
plan: "02"
type: execute
wave: 2
depends_on:
  - plan: "01"
files_modified:
  - src/tree.ts
  - src/tree.test.ts
autonomous: true
requirements:
  - LAYER-03
user_setup: []

must_haves:
  truths:
    - "Path resolver calculates full node ancestor list up to root element"
    - "TypeScript compilation passes for tree module additions"
  artifacts:
    - path: src/tree.ts
      provides: "getNestingPath ancestor resolver"
      contains: "getNestingPath"
    - path: src/tree.test.ts
      provides: "Breadcrumb matching tests"
      contains: "getNestingPath"
---

<objective>
Implement the ancestor path resolver (`getNestingPath`) in `src/tree.ts` to locate selected elements in the component tree and return their breadcrumbs path (list of tags from root to selection).
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-layers-panel-and-reordering/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement breadcrumb nesting finder</name>
  <files>src/tree.ts</files>
  <action>Add getNestingPath to src/tree.ts. This function takes the component tree and a target coordinate ID, searches the hierarchy, and returns the array of ancestor node names leading to it.</action>
  <verify>Check tree.ts compiles</verify>
  <acceptance_criteria>
    - src/tree.ts compiles successfully
    - contains getNestingPath function
    - returns tag path arrays (e.g., ['div', 'section', 'button'])
  </acceptance_criteria>
  <done>Breadcrumb nesting finder implemented</done>
</task>

<task type="auto">
  <name>Task 2: Write tests for getNestingPath</name>
  <files>src/tree.test.ts</files>
  <action>Add unit tests in src/tree.test.ts simulating multi-level component trees and asserting correct breadcrumb outputs for various selection coordinates.</action>
  <verify>Run tests using npm run test to verify success</verify>
  <acceptance_criteria>
    - src/tree.test.ts includes nesting path tests
    - all tests pass
  </acceptance_criteria>
  <done>Nesting path tests implemented and passing</done>
</task>

</tasks>

<verification>
- [ ] TypeScript type checks succeed
- [ ] Test command `npm run test` executes successfully and reports all tests passing
</verification>

<success_criteria>
- Selection breadcrumbs correctly list ancestor tags
- Tests confirm LAYER-03 is satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/07-layers-panel-and-reordering/07-02-SUMMARY.md`
</output>
