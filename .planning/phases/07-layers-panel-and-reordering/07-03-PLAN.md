---
phase: 07-layers-panel-and-reordering
plan: "03"
type: execute
wave: 3
depends_on:
  - plan: "02"
files_modified:
  - src/reorder.ts
  - src/reorder.test.ts
autonomous: true
requirements:
  - LAYER-02
user_setup: []

must_haves:
  truths:
    - "AST reordering handler shifts JSX elements non-destructively"
    - "TypeScript compilation passes for reorder module"
  artifacts:
    - path: src/reorder.ts
      provides: "AST mutator for reordering JSX elements"
      contains: "reorderJSXElement"
    - path: src/reorder.test.ts
      provides: "Unit tests validating element moves and parent swaps"
      contains: "describe"
---

<objective>
Implement the element reordering AST mutation function (`reorderJSXElement`) in `src/reorder.ts` to reposition target elements inside the same parent or move them to a new parent in the JSX layout tree.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-layers-panel-and-reordering/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement reordering AST mutator</name>
  <files>src/reorder.ts</files>
  <action>Create src/reorder.ts. Parse code into AST. Locate the target element and its parent by coordinates. Move the target element node within the parent children list or insert it into a different parent, and generate code output.</action>
  <verify>Check reorder.ts compiles</verify>
  <acceptance_criteria>
    - src/reorder.ts exists and compiles successfully
    - contains reorderJSXElement function
    - correctly moves elements inside the JSX structure
  </acceptance_criteria>
  <done>Reordering AST mutator implemented</done>
</task>

<task type="auto">
  <name>Task 2: Write tests for reordering</name>
  <files>src/reorder.test.ts</files>
  <action>Create src/reorder.test.ts verifying repositioning sibling elements and moving an element under a new parent tag.</action>
  <verify>Run tests using npm run test to verify success</verify>
  <acceptance_criteria>
    - src/reorder.test.ts exists
    - tests verify reordered code matches expected outputs
    - all tests pass
  </acceptance_criteria>
  <done>Reordering tests implemented and passing</done>
</task>

</tasks>

<verification>
- [ ] TypeScript type checks succeed
- [ ] Test command `npm run test` executes successfully and reports all tests passing
</verification>

<success_criteria>
- Reordering operations generate correct mutated source code
- Tests confirm LAYER-02 is satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/07-layers-panel-and-reordering/07-03-SUMMARY.md`
</output>
