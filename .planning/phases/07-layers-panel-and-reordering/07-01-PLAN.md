---
phase: 07-layers-panel-and-reordering
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tree.ts
  - src/tree.test.ts
autonomous: true
requirements:
  - LAYER-01
user_setup: []

must_haves:
  truths:
    - "Component tree traverses JSX/TSX structures successfully"
    - "TypeScript compilation passes for tree module"
  artifacts:
    - path: src/tree.ts
      provides: "AST traverser compiling JSX tree hierarchy representation"
      contains: "buildComponentTree"
    - path: src/tree.test.ts
      provides: "Unit tests validating element mapping and children extraction"
      contains: "describe"
---

<objective>
Implement JSX/TSX element tree parsing engine (`buildComponentTree`) in `src/tree.ts` that traverses the source code AST and returns a nested component tree JSON structure.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-layers-panel-and-reordering/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement component tree parser</name>
  <files>src/tree.ts</files>
  <action>Create src/tree.ts. Parse code into AST using Babel. Traverse the AST to build a hierarchy of JSXElement nodes matching their data-cf-source attributes as IDs, and return the nested tree structure.</action>
  <verify>Check tree.ts compiles</verify>
  <acceptance_criteria>
    - src/tree.ts exists and compiles successfully
    - contains buildComponentTree method
    - parses nested tags correctly (div > button)
  </acceptance_criteria>
  <done>Component tree parser implemented</done>
</task>

<task type="auto">
  <name>Task 2: Write tests for tree builder</name>
  <files>src/tree.test.ts</files>
  <action>Create src/tree.test.ts and verify nested trees are constructed accurately from JSX snippets.</action>
  <verify>Run tests using npm run test to verify success</verify>
  <acceptance_criteria>
    - src/tree.test.ts exists
    - tests verify nested JSX tags are parsed into matching parent-child arrays
    - all tests pass
  </acceptance_criteria>
  <done>Tree builder tests implemented and passing</done>
</task>

</tasks>

<verification>
- [ ] TypeScript type checks succeed
- [ ] Test command `npm run test` executes successfully and reports all tests passing
</verification>

<success_criteria>
- Component tree maps exactly to file JSX hierarchies
- Tests confirm LAYER-01 is satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/07-layers-panel-and-reordering/07-01-SUMMARY.md`
</output>
