---
phase: 11-polish-asset-panel-and-svelte-adapter
plan: "03"
type: execute
wave: 3
depends_on:
  - plan: "02"
files_modified:
  - src/snap.ts
  - src/snap.test.ts
autonomous: true
requirements:
  - OVERLAY-03
user_setup: []

must_haves:
  truths:
    - "Grid snapping function correctly rounds coordinate values to nearest grid interval"
    - "TypeScript compilation passes for snap module"
  artifacts:
    - path: src/snap.ts
      provides: "Canvas coordinates grid snapping helper"
      contains: "snapToGrid"
    - path: src/snap.test.ts
      provides: "Unit tests verifying rounded alignment values"
      contains: "describe"
---

<objective>
Implement coordinates grid snapping utility (`snapToGrid`) in `src/snap.ts` to align dragged canvas coordinates to specific grid sizes (defaulting to 8px grids).
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-polish-asset-panel-and-svelte-adapter/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement coordinates grid snapping</name>
  <files>src/snap.ts</files>
  <action>Create src/snap.ts. Write snapToGrid function which takes coordinate values and rounds them to the nearest grid step size.</action>
  <verify>Check snap.ts compiles</verify>
  <acceptance_criteria>
    - src/snap.ts exists and compiles successfully
    - contains snapToGrid function
  </acceptance_criteria>
  <done>Grid snapping utility implemented</done>
</task>

<task type="auto">
  <name>Task 2: Write tests for grid snapping</name>
  <files>src/snap.test.ts</files>
  <action>Create src/snap.test.ts verifying snapping positions for various dragged layout coordinate cases.</action>
  <verify>Run tests using npm run test to verify success</verify>
  <acceptance_criteria>
    - src/snap.test.ts exists
    - tests verify coordinate snapping alignments
    - all tests pass
  </acceptance_criteria>
  <done>Grid snapping tests implemented and passing</done>
</task>

</tasks>

<verification>
- [ ] TypeScript type checks succeed
- [ ] Test command `npm run test` executes successfully and reports all tests passing
</verification>

<success_criteria>
- Drag coordinates snap cleanly to grid lines
- Tests confirm OVERLAY-03 is satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/11-polish-asset-panel-and-svelte-adapter/11-03-SUMMARY.md`
</output>
