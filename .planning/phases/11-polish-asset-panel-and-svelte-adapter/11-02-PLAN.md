---
phase: 11-polish-asset-panel-and-svelte-adapter
plan: "02"
type: execute
wave: 2
depends_on:
  - plan: "01"
files_modified:
  - src/assets.ts
  - src/assets.test.ts
autonomous: true
requirements:
  - ASSET-01
user_setup: []

must_haves:
  truths:
    - "Asset saver copies files to the public assets directory and resolves target paths"
    - "TypeScript compilation passes for assets module"
  artifacts:
    - path: src/assets.ts
      provides: "Local asset upload helper"
      contains: "saveUploadedAsset"
    - path: src/assets.test.ts
      provides: "Unit tests verifying directory creation and file copies"
      contains: "describe"
---

<objective>
Implement the asset copying and path resolver helper (`saveUploadedAsset`) in `src/assets.ts` to support rendering locally saved images in user designs.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-polish-asset-panel-and-svelte-adapter/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement asset saving utility</name>
  <files>src/assets.ts</files>
  <action>Create src/assets.ts. Write saveUploadedAsset which accepts root directory, source file path, and destination filename. Copy file to root/public/assets/ and return relative URL path.</action>
  <verify>Check assets.ts compiles</verify>
  <acceptance_criteria>
    - src/assets.ts exists and compiles successfully
    - contains saveUploadedAsset function
  </acceptance_criteria>
  <done>Asset saving utility implemented</done>
</task>

<task type="auto">
  <name>Task 2: Write tests for asset saving utility</name>
  <files>src/assets.test.ts</files>
  <action>Create src/assets.test.ts. Mock node fs modules to verify directory creation, copying, and relative path resolutions.</action>
  <verify>Run tests using npm run test to verify success</verify>
  <acceptance_criteria>
    - src/assets.test.ts exists
    - tests verify file copying and path resolution
    - all tests pass
  </acceptance_criteria>
  <done>Asset saving utility tests implemented and passing</done>
</task>

</tasks>

<verification>
- [ ] TypeScript type checks succeed
- [ ] Test command `npm run test` executes successfully and reports all tests passing
</verification>

<success_criteria>
- Asset copy and file saving is fully verified
- Tests confirm ASSET-01 is satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/11-polish-asset-panel-and-svelte-adapter/11-02-SUMMARY.md`
</output>
