---
phase: 11-polish-asset-panel-and-svelte-adapter
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/svelte.ts
  - src/svelte.test.ts
autonomous: true
requirements:
  - ADAP-03
user_setup: []

must_haves:
  truths:
    - "Svelte SFC modifier correctly updates class values on targeted template tags"
    - "TypeScript compilation passes for svelte module"
  artifacts:
    - path: src/svelte.ts
      provides: "Svelte SFC class transformer"
      contains: "updateSvelteClass"
    - path: src/svelte.test.ts
      provides: "Unit tests validating Svelte template tag class updates"
      contains: "describe"
---

<objective>
Implement the Svelte component class rewriter (`updateSvelteClass`) in `src/svelte.ts` to locate and replace class names on element tags matching active coordinates.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-polish-asset-panel-and-svelte-adapter/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Svelte SFC class rewriter</name>
  <files>src/svelte.ts</files>
  <action>Create src/svelte.ts. Write updateSvelteClass which regex matches the element carrying data-cf-source equal to targetId and modifies/injects class attributes.</action>
  <verify>Check svelte.ts compiles</verify>
  <acceptance_criteria>
    - src/svelte.ts exists and compiles successfully
    - contains updateSvelteClass function
  </acceptance_criteria>
  <done>Svelte class rewriter implemented</done>
</task>

<task type="auto">
  <name>Task 2: Write tests for Svelte class rewriter</name>
  <files>src/svelte.test.ts</files>
  <action>Create src/svelte.test.ts. Verify replacing class names and preserving surrounding script setup/style blocks in Svelte markup.</action>
  <verify>Run tests using npm run test to verify success</verify>
  <acceptance_criteria>
    - src/svelte.test.ts exists
    - tests verify class name modifications in Svelte components
    - all tests pass
  </acceptance_criteria>
  <done>Svelte class rewriter tests implemented and passing</done>
</task>

</tasks>

<verification>
- [ ] TypeScript type checks succeed
- [ ] Test command `npm run test` executes successfully and reports all tests passing
</verification>

<success_criteria>
- Svelte template elements are mutated safely
- Tests confirm ADAP-03 is satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/11-polish-asset-panel-and-svelte-adapter/11-01-SUMMARY.md`
</output>
